---
name: Agent 目录治理
version: 2.3.0
last_updated: 2026-06-07
---

# Agent 目录治理

- `memory/`：长期稳定规则，仅直接 `.md` 被加载。
- `skills/`：技能定义与私有辅助文件。
- `jobs/`：每个 Job 一个目录，只放 `JOB.md`。
- `scripts/`：确定性维护脚本。
- `runtime/`：运行态、缓存、当前生效配置、密钥等，不进 Git 能力资产。也承接不应进入长期记忆、但需要被特定技能读取的运行锚点，如仓库 SSH 映射、Host 别名、公钥指纹、本地路径。
- `docs/`：历史资料、归档、说明，不作为运行态来源。
- `activity/`：自动活动日志，只读。

新增/迁移文件按“它是什么”放置；禁止把缓存放 memory/docs，禁止把脚本放 jobs，禁止把单次过程写长期记忆。若某经验只在特定任务触发时才有价值，应优先蒸馏进 skill，而不是常驻 memory。减负治理优先缩小常驻面，避免把审计过程、路径清单或低频细节写入 memory。
